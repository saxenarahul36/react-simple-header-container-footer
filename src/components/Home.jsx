import React from "react";
import styled from "styled-components";

export const HomContainer = styled.div``;

const Home = () => {
  return (
    <HomContainer>
      <span style={{ fontSize: "50px" }}>Welcome Home Page</span>
      <br />
    </HomContainer>
  );
};

export default Home;
